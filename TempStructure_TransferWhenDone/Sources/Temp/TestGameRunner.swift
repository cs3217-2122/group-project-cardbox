class TestGameRunner: GameRunner<TestCard,TestPlayer> {
    func refreshGame() {
        // TODO: refresh game whenever new things added (?)
    }

    func update<T: GameEvent>(gameEvent: T) {
    }
}
